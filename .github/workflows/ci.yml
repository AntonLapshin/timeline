# CI pipeline for timeline (generated by auto-pi, Milestone 4 / plan.md §11.1).
#
# Runs on every push to `main` (and on pull requests): installs dependencies,
# lints, runs the test suite with 100% core coverage enforcement, builds the
# production bundle, and uploads the `dist/` artifact for later use (e.g. by
# the Pages deploy workflow).

name: CI

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test with coverage
        run: npm run test:coverage

      - name: Build
        run: npm run build

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          if-no-files-found: error
