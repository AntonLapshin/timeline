{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "https://auto-pi.dev/schemas/config.schema.json",
  "$comment": "auto-pi config schema reference. Validate .pi/config.json against the harness schema (config/config.schema.json in the auto-pi repo)."
}
